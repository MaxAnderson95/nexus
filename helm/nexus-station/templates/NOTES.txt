NEXUS Station has been deployed successfully!

{{- if .Values.cortex.enabled }}

Cortex (BFF) is available at:
  http://{{ include "nexus-station.fullname" . }}-cortex:{{ .Values.cortex.service.port }}
{{- end }}

{{- if .Values.postgres.enabled }}

PostgreSQL is available at:
  {{ include "nexus-station.fullname" . }}-postgres:{{ .Values.postgres.service.port }}
{{- end }}

{{- if .Values.redis.enabled }}

Redis is available at:
  {{ include "nexus-station.fullname" . }}-redis:{{ .Values.redis.service.port }}
{{- end }}

Microservices:
{{- if .Values.dockingService.enabled }}
  - Docking Service: http://{{ include "nexus-station.fullname" . }}-docking-service:{{ .Values.dockingService.service.port }}
{{- end }}
{{- if .Values.crewService.enabled }}
  - Crew Service: http://{{ include "nexus-station.fullname" . }}-crew-service:{{ .Values.crewService.service.port }}
{{- end }}
{{- if .Values.lifeSupportService.enabled }}
  - Life Support Service: http://{{ include "nexus-station.fullname" . }}-life-support-service:{{ .Values.lifeSupportService.service.port }}
{{- end }}
{{- if .Values.powerService.enabled }}
  - Power Service: http://{{ include "nexus-station.fullname" . }}-power-service:{{ .Values.powerService.service.port }}
{{- end }}
{{- if .Values.inventoryService.enabled }}
  - Inventory Service: http://{{ include "nexus-station.fullname" . }}-inventory-service:{{ .Values.inventoryService.service.port }}
{{- end }}

{{- if .Values.loadGenerator.enabled }}

Load Generator is running with {{ .Values.loadGenerator.users }} users.
{{- end }}
