apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-station.fullname" . }}-crew-service
  labels:
    {{- include "nexus-station.componentLabels" (dict "root" . "component" "crew-service") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.crewService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-station.componentSelectorLabels" (dict "root" . "component" "crew-service") | nindent 4 }}
