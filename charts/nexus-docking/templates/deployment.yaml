apiVersion: apps/v1
kind: Deployment
metadata:
  name: docking
  labels:
    {{- include "nexus-docking.labels" . | nindent 4 }}
    app.kubernetes.io/component: docking
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  strategy:
    {{- toYaml .Values.strategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "nexus-docking.selectorLabels" (dict "context" . "component" "docking") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nexus-docking.selectorLabels" (dict "context" . "component" "docking") | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- include "nexus-docking.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: docking
          image: {{ include "nexus-docking.image" (dict "context" . "image" .Values.image) | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.containerPort }}
              protocol: TCP
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: SERVICE_NAME
              value: "docking-service"
            - name: SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA
              value: {{ .Values.database.schema | quote }}
            {{- include "nexus-common.loggingEnv" . | nindent 12 }}
            {{- include "nexus-common.podEnv" . | nindent 12 }}
            {{- include "nexus-common.databaseEnv" . | nindent 12 }}
            {{- include "nexus-common.redisEnv" . | nindent 12 }}
            {{- include "nexus-common.otelEnv" . | nindent 12 }}
            {{- include "nexus-common.chaosEnv" . | nindent 12 }}
            - name: SERVICES_POWER_URL
              value: {{ include "nexus-docking.powerUrl" . | quote }}
            - name: SERVICES_CREW_URL
              value: {{ include "nexus-docking.crewUrl" . | quote }}
            - name: SERVICES_INVENTORY_URL
              value: {{ include "nexus-docking.inventoryUrl" . | quote }}
            {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
