{{- if .Values.dockingService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-station.fullname" . }}-docking-service
  labels:
    {{- include "nexus-station.labels" . | nindent 4 }}
    app.kubernetes.io/component: docking-service
spec:
  type: {{ .Values.dockingService.service.type }}
  ports:
    - port: {{ .Values.dockingService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-station.selectorLabels" (dict "context" . "component" "docking-service") | nindent 4 }}
{{- end }}
